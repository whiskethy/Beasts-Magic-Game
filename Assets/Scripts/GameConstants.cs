using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public struct GameConstants{

    enum Level
    {
        StartMenu = 0,
        MainWorld = 1,
        NetworkedWorld = 2
    }
	
}
